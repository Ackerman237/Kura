# Peta Padanan JavaScript: WibuDex vs Kura Manga & Anime Reader

Tabel ini memetakan implementasi logika client-side antara patokan kebenaran (**WibuDex** branch `restructure-product-naming` di `public/video/js/`) dan project ini (**Kura**).

| Fungsi / File di WibuDex (`public/video/js/`) | Padanan di Project Ini | Beda Perilaku Kunci | Terkait Masalah |
| :--- | :--- | :--- | :--- |
| **`cards.js`** (`renderMediaCard`)<br>• Menggunakan atribut `referrerpolicy="no-referrer"` pada `<img>`<br>• Menggunakan `onerror="this.src=PLACEHOLDER"`<br>• Link tujuan `/watch.html?slug=...` | `src/web/components/common/VideoCard.vue`, `CinemaTrendingRail.vue`, `HomeContinueReadingRail.vue`, `LibraryHistoryTab.vue` | • `CinemaTrendingRail.vue`, `HomeContinueReadingRail.vue`, dan `LibraryHistoryTab.vue` **TIDAK** menyertakan `referrerpolicy="no-referrer"`, menyebabkan gambar NekoPoi diblokir 403 Forbidden oleh ISP/upstream anti-hotlink.<br>• `storage.js` pada saat `saveReadingProgress` **TIDAK** menyimpan `thumb` atau `cover`, sehingga `HomeContinueReadingRail` dan `LibraryHistoryTab` selalu menerima `item.thumb = undefined` (gambar kosong melompong). | **Masalah 1** (Cover tidak muncul) |
| **`watch.js`** (`mountPlayer`, `mountDirectFrame`, `isAllowedPlayerUrl`)<br>• Menggunakan daftar `playerAllowedHosts` (`playmogo.com`, `streampoi.com`, `yandex.ru`, `nhplayer.com`).<br>• Bila URL termasuk allowed host, merender `<iframe>` langsung tanpa sandbox ketat agar cookie session dan anti-tamper provider berfungsi normal.<br>• Menggunakan fallback anggun bila mode gagal. | `src/web/components/video-watch/WatchPlayerContainer.vue`, `src/server/routes/playerFrame.js` | • Project ini selalu membungkus iframe lewat `/api/video/player-frame`.<br>• `playerFrame.js` memodifikasi `XMLHttpRequest.prototype.open` dan meniadakan `window.open`, serta menerapkan CSP sandbox tanpa `allow-same-origin`.<br>• Akibatnya, script verifikasi `nhplayer.com` (`player-core-v2.php`) mendeteksi adanya manipulasi/blokir cookie, memicu `onVideoUrlError()`, dan menampilkan tulisan merah *"Failed to load video. Please refresh"* saat tombol play ditekan. | **Masalah 2** (Player HentaiTV gagal putar) |
| **`watch.js`** & **`index.js`** (URL Parameter Parsing)<br>• URL query parameters (`?slug=...`, `?category=...`, `?page=...`) adalah Single Source of Truth.<br>• History pushState/replaceState menyimpan parameter lengkap termasuk filter/tab. | `src/web/composables/useNavigation.js`, `src/web/App.vue`, `src/web/sw.js` | • `App.vue` menyimpan `videoProvider` di memori komponen (`ref('htv')`) dan **TIDAK** menyertakan parameter `provider` di URL (`?tab=video&view=video-watch&slug=...`).<br>• Saat dilakukan refresh (F5/Ctrl+Shift+R), `App.vue` membaca slug tetapi mengasumsikan provider adalah `htv`. Jika video berasal dari NekoPoi atau Eporner, pemanggilan detail gagal (404/502).<br>• Service Worker (`sw.js`) mencocokkan request SPA HTML menggunakan URL lengkap tanpa `ignoreSearch: true`, sehingga bila offline/revalidating dapat mengembalikan respons usang/kosong. | **Masalah 3** (Refresh membuat halaman pindah/error) |
| **`lib/scraper/neko/http.js`** & **`neko-scraper.js`**<br>• Menghubungi `https://nekopoi.care` menggunakan DoH / VPN routing.<br>• Parser `parseCards` mengekstrak `nk-thumb-crop` dan link `<h2><a>` dengan benar. | `src/sources/nekopoi/client.js`, `src/sources/eporner/client.js`, `src/http.js` | • Server project ini memanggil `safeFetch` dengan resolver DNS bawaan OS Windows. Di Indonesia, domain `nekopoi.care` dan `eporner.com` terkena DNS poisoning ISP dan diarahkan ke IP blocking `36.86.63.185` (`internetpositif.id`), yang sertifikat SSL-nya telah kedaluwarsa (Juni 2026), memicu `CERT_HAS_EXPIRED` / `handshake failure`.<br>• `eporner.js` menggunakan endpoint `api.eporner.com` yang meredirect (301) ke `www.eporner.com` dan proxy gratisan yang mati. Begitu menggunakan resolver DoH Cloudflare dan endpoint resmi `www.eporner.com`, respons HTTP 200 normal didapatkan. | **Masalah 4** (Top trending selain HentaiTV error) |
| **`lib/scraper/neko/text.js`** (`cleanText`, `decodeEntities`)<br>• `decodeEntities` dan decoding UTF-8 yang bersih untuk teks multibahasa (Jepang/CJK).<br>• Font CSS di WibuDex menggunakan font sistem lengkap. | `src/web/styles/tokens.css`, `src/http.js`, `src/sources/nekopoi/parser.js` | • `--kura-font-sans` dan `--kura-font-heading` di `tokens.css` hanya memuat font Latin (`Outfit`, `Plus Jakarta Sans`, `Roboto`, `sans-serif`) tanpa fallback font CJK (`Hiragino Sans`, `Yu Gothic`, `Meiryo`, `PingFang SC`, `Microsoft YaHei`, `Noto Sans CJK`). Pada sistem tertentu, teks Jepang/Mandarin/Korea tampil kotak-kotak (tofu □).<br>• Parser entities di scraper tidak menangani numeric unicode HTML entities (misal `&#26085;`). | **Masalah 5** (Font CJK / encoding simbol aneh) |
| **`watch.js`** (`renderSidebarList`)<br>• Rekomendasi / episode diambil langsung dari `detail.episodes` dan `detail.related` milik data video yang sedang dibuka (berasal dari provider yang sama). | `src/web/App.vue` (baris 747), `src/web/views/VideoWatchView.vue` | • `App.vue` meneruskan `:related-videos="videoList.filter(...)"` ke `VideoWatchView`. `videoList` di `App.vue` di-hardcode dari katalog HentaiTV (provider `'htv'`).<br>• Akibatnya, baik video NekoPoi maupun Eporner selalu menerima daftar rekomendasi dari HentaiTV. Rekomendasi harus diambil dari provider video yang sedang aktif atau dari detail provider tersebut. | **Masalah 6** (Video serupa selalu dari HentaiTV) |
